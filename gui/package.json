{
  "name": "gui",
  "version": "1.0.0",
  "description": "",
  "main": "main/index.js",
  "scripts": {
    "start": "set NODE_ENV=dev && electron .",
    "build": "set NODE_ENV=pro && npx @electron/packager . MyApp --platform=win32 --arch=x64 --asar --out=dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.3.2",
    "electron": "^30.0.0",
    "electron-reloader": "^1.2.3"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "hmc-win32": "^1.4.9",
    "screenshot-desktop": "^1.15.0"
  }
}
